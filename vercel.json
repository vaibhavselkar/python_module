{
  "version": 2,
  "builds": [
    {
      "src": "/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/feedback",
      "dest": "/server.js",
      "method": ["GET", "PUT", "POST"]
    }    
  ]
}
